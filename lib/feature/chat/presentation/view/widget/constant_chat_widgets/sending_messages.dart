import 'package:flutter/material.dart';

class SendingMessages extends StatelessWidget {
  const SendingMessages({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
